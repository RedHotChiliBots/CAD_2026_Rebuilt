# CAD_2026_Rebuilt

**CAD source of truth:** whatever the team is using (e.g., Onshape / SolidWorks).  
This repo exists to store CAD files / exports and keep history in one place.

## Git LFS (required)
This repo uses **Git LFS** for CAD binaries and exports (SolidWorks, STEP, STL, DXF, PDF, etc.).

### One-time setup (per computer)
1) Install Git LFS
2) Run:
   ```bash
   git lfs install

